import 'package:flutter_test/flutter_test.dart';
import 'package:agrismartai/main.dart';

void main() {
  test('App colors defined', () {
    expect(C.primary, isNotNull);
    expect(C.secondary, isNotNull);
  });
}
